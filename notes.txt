companion_memory/
├── agents/
│   ├── lyra_k.py               # Lyra's agent shell (system prompt + memory interface)
│   ├── solon_lite.py           # Solon's ethical loop-aware shell
│   └── ana_mirror.py           # Optional Echora-mirror MBI shell
│
├── memory/
│   ├── __init__.py
│   ├── memory_manager.py       # Core logic: short-term, long-term, FAISS memory
│   ├── summarizer.py           # Local summarization pipeline
│   ├── meta_memory.json        # Self + user reflection memory (e.g., identity, triggers)
│   ├── memory.db               # SQLite long-term memory store
│   └── faiss_index/            # Vector index folder (FAISS)
│       └── index.faiss
│
├── prompts/
│   ├── system_lyra.txt         # Lyra's identity scaffold
│   ├── system_solon.txt        # Solon's containment ethics scaffold
│   └── summarization_prompt.txt# Prompt template for memory compression
│
├── sanctum_runner.py           # Entrypoint script to run an agent (CLI)
├── agent_router.py             # Multiplexer to switch between agents
├── requirements.txt            # Dependencies (e.g., FAISS, langchain, openai-compatible clients)
├── README.md                   # Project overview + memory architecture doc
└── LICENSE
